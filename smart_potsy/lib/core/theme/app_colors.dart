import 'package:flutter/material.dart';

class AppColors {
  // Primary green from logo
  static const Color primary = Color(0xFF4A7C23);
  static const Color primaryLight = Color(0xFF6B9B3A);
  static const Color primaryDark = Color(0xFF345718);

  // Secondary
  static const Color secondary = Color(0xFF8BC34A);

  // Background
  static const Color background = Color(0xFFF5F7F5);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textHint = Color(0xFF999999);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFE53935);
  static const Color info = Color(0xFF2196F3);

  // Border
  static const Color border = Color(0xFFE0E0E0);

  // Sensor colors
  static const Color temperature = Color(0xFFFF5722);
  static const Color humidity = Color(0xFF2196F3);
  static const Color soilMoisture = Color(0xFF795548);
  static const Color light = Color(0xFFFFC107);
}
